@using Sports.MVC.Models.Concrete
@model TeamViewModel

@{
	int id = Model.Id;
}

<div class="card">
	<div class="card-header">
		YENİ
	</div>
	<div class="card-body">
		<form class="row g-3" asp-controller="Team" asp-action="Edit">
			<div class="col-md-12">
				<label asp-for="TeamName" class="form-label">Adı</label>
				<input asp-for="TeamName" class="form-control" />
			</div>
			<div class="col-md-12">
				<label asp-for="FoundedYear" class="form-label">Adı</label>
				<input asp-for="FoundedYear" class="form-control" />
			</div>
			<div class="col-md-12">
				<label asp-for="City" class="form-label">Adı</label>
				<input asp-for="City" class="form-control" />
			</div>

			<div class="col-12">
				<input type="submit" class="btn btn-success" value="Düzenle" />
				<input type="hidden" asp-for="Id" />
			</div>
		</form>
	</div>
	<div class="card-footer text-body-secondary">
		<span class="badge text-secondary">@DateTime.Now.ToString("dd/MM/yyyy HH:mm:ss")</span>
	</div>
</div>

@section Scripts
{
	<partial name="_ValidationScriptsPartial" />
}