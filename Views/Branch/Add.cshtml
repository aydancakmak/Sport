@using Sports.MVC.Models.Concrete
@model BranchViewModel

<div class="card">
	<div class="card-header">
		YENİ
	</div>
	<div class="card-body">
		<form class="row g-3" asp-controller="Branch" asp-action="Add" method="post">
			<div class="col-md-12">
				<label asp-for="Name" class="form-label"></label>
				<input asp-for="Name" class="form-control" />
				<span asp-validation-for="Name" class="text-danger"></span>
			</div>

			
			<div class="col-12">
				<input type="submit" class="btn btn-success" value="Ekle" />
			</div>
		</form>
	</div>
	<div class="card-footer text-body-secondary">
		<span class="badge text-info">@DateTime.Now.ToString("dd/MM/yyyy HH:mm:ss")</span>
	</div>
</div>

@section Scripts
{
	<partial name="_ValidationScriptsPartial" />
}